#include <stdio.h>
/**
 * main - entry point.
 * Return: 0 is success.
 */
int main(void)
{
char ch;
for (ch = 'a'; ch = 'z'; ch++)
{
if (ch != 'e' && ch != 'q')
puthar(ch);
}
putchar('\n');
return (0);
}
