#include <stdio.h>
/**
 * main - entry point
 * Return (0) success
 */
int main(void)
{
char ch, e, q;
e = 'e';
q = 'q';
for (ch = 'a' ; ch <= 'z' ; ch++);
{
if (ch != e && ch != q'\n')
putchar(ch);
}
putchar('\n');
return (0);
}
